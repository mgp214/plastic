import 'package:flutter/material.dart';

class Constants {
  static final snackDuration = Duration(milliseconds: 4000);
  static const double _borderRadius = 3;
  static final Radius borderRadius = Radius.circular(_borderRadius);
  static final double iconSize = 40;
  static final double secondaryIconSize = 20;
}
